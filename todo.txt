- Write library of all common mathematical functions that are used in code, but defined on each file independently, such as factorial, binomial and vector of binomials.
- Correct Rmnk to use a double approximation of factorial
- Implement class of polynomials/tensors stored with 3 indices in sym_space to implement operators with weight dependant definitions. Change get_all_gmnk to use this
- Parallelize get_all_gmnk. First it would be optimal to solve the previous task
- Debug why kravchuk_exp tend to crash when n_qubits = 5, 7, 9
- Implement right multiplication of GF2N matrix by a vector in GF2N. Either implement the inefficient version, assume matrix is symmetric or store both the matrix and the transpose matrix 
- Change kravchuk_exp to only compute the buffers when they are necessary, so that no memory is used whenever a kravchuk_exp is created, until evaluation forces the creation. Succesive evaluations check if computed to not compute again
- Modify all classes to use a particular convention for member variables. Correct all functions that had conflicts with repeated names, such as n_nodes for simple_graph
- Move graph class to general library
- Use CMake
- Refactor all classes that store arrays as std::unique_ptr, as they are points of failure and have close to no benefits
- Allow GF2N_matrix to be resized, then implement copy assignment operators for simple_graph
- Optimize Qfunc with the new formulas and remove the stack buffers in favor of cached buffers
- Implement looping over all displaced symQ with the optimized displaced symQ formula?
- Implement looping over graph_states with a certain number of random graphs